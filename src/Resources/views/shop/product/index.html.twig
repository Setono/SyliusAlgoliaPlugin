{% extends "@SyliusShop/layout.html.twig" %}

{% block content %}
    {{ sylius_template_event('sylius.shop.taxon.header', {'taxon': taxon}) }}

    <div id="searchbox"></div>
    <div id="range-slider"></div>
    <div id="refinement-list"></div>
    <div id="jeans-size"></div>
    <div id="t-shirt-size"></div>
    <div id="hits"></div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script src="https://cdn.jsdelivr.net/npm/algoliasearch@4.5.1/dist/algoliasearch-lite.umd.js" integrity="sha256-EXPXz4W6pQgfYY3yTpnDa3OH8/EPn16ciVsPQ/ypsjk=" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/instantsearch.js@4.8.3/dist/instantsearch.production.min.js" integrity="sha256-LAGhRRdtVoD6RLo2qDQsU2mp+XVSciKRC8XPOBWmofM=" crossorigin="anonymous"></script>
    <script>
        const searchClient = algoliasearch('2O6CZAU8ZC', '1e84639c29f5871b4e5caab86b20d0ca');

        const search = instantsearch({
            indexName: '{{ index }}',
            searchClient,
            routing: true,
        });

        search.addWidgets([
            instantsearch.widgets.searchBox({
                container: '#searchbox',
            }),

            instantsearch.widgets.hits({
                container: '#hits',
                templates: {
                    item: `{% include "@SetonoSyliusAlgoliaPlugin/shop/product/_item.html.twig" %}`,
                }
            }),
            instantsearch.widgets.rangeSlider({
                container: '#range-slider',
                attribute: 'basePrice',
            }),
            instantsearch.widgets.refinementList({
                container: '#refinement-list',
                attribute: 'taxons',
            }),
            instantsearch.widgets.refinementList({
                container: '#jeans-size',
                attribute: 'options.Jeans size',
            }),
            instantsearch.widgets.refinementList({
                container: '#t-shirt-size',
                attribute: 'options.T-shirt size',
            }),
            instantsearch.widgets.configure({
                filters: "taxonCodes:{{ taxon.code }}"
            })
        ]);

        search.start();
    </script>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/instantsearch.css@7.4.5/themes/satellite-min.css" integrity="sha256-TehzF/2QvNKhGQrrNpoOb2Ck4iGZ1J/DI4pkd2oUsBc=" crossorigin="anonymous">
    <style>
        .ais-Hits-list {
            display: grid;
            grid-gap: 40px;
            grid-template-columns: 1fr;
        }

        @media (min-width: 680px) {
            .ais-Hits-list {
                grid-template-columns: 1fr 1fr;
            }
        }

        @media (min-width: 900px) {
            .ais-Hits-list {
                grid-template-columns: 1fr 1fr 1fr;
            }
        }

        @media (min-width: 1200px) {
            .ais-Hits-list {
                grid-template-columns: 1fr 1fr 1fr 1fr;
            }
        }
    </style>
{% endblock %}
